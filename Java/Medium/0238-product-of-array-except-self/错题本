
🐛 Bug 在最后的取结果循环
复制
for (int i = k - 1; i > 0; i--) {  // ❌ i > 0，漏掉了 i = 0
    result[i] = pq.poll();
}
循环条件是 i > 0，所以 i = 0 这一轮永远不会执行
result[0] 永远是默认值 0，少取了一个元素！

✅ 修复：改成 i >= 0
复制
for (int i = k - 1; i >= 0; i--) {  // ✅
    result[i] = pq.poll();
}
🔍 为什么要从 k-1 倒着取？
因为用的是 Min-Heap（最小堆），堆顶是频率最小的元素。

poll() 每次弹出的是频率从小到大的顺序，所以倒着填入数组，最终结果就是频率从大到小排列：

复制
pq 内部（频率小→大）：[freq=1, freq=2, freq=3]

i=2: result[2] = poll() → 频率最小的
i=1: result[1] = poll() → 频率中等的
i=0: result[0] = poll() → 频率最大的  ← 这一步之前被漏掉了！
