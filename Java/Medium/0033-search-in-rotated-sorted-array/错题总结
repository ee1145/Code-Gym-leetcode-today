错题本总结

🔴 错误1：返回值搞错（第一版）
复制
returnn nums[mid];  // 两个错：拼写 + 返回了值而不是下标
return mid;         // ✅ 题目要的是 index
根因：没有仔细看题目要求返回什么。

🔴 错误2：改错了变量（第一版）
复制
mid = right - 1;  // ❌ 改 mid 没有意义，下一轮会重新计算
right = mid - 1;  // ✅ 二分查找移动的永远是 left 和 right
根因：对二分查找的核心机制不熟悉——缩小范围靠移动 left/right，不是 mid。

🔴 错误3：外层判断条件搞错（第一版）
复制
if (nums[left] <= target)    // ❌ 判断了 target，但这里应该判断结构
if (nums[left] <= nums[mid]) // ✅ 判断的是"哪半段有序"
根因：没理解外层 if 的目的是判断数组结构，不是判断 target。
